import {
  "mizchi/bit" as @git,
  "mizchi/bit/lib" as @lib,
  "moonbitlang/core/encoding/utf8" as @utf8,
}

import "test" {
  "mizchi/bit/lib" as @lib,
}

options(
  targets: { "sync_wbtest.mbt": [ "native" ] },
  "warn-list": "-29",
)