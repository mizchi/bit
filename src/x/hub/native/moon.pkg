import {
  "mizchi/bit" as @git,
  "mizchi/bit/pack" as @pack,
  "mizchi/bit/protocol" as @protocol,
  "mizchi/bit/native" as @gitnative,
  "mizchi/bit/lib" as @lib,
  "mizchi/bit/x/hub" as @hub,
  "moonbitlang/async" as @async,
  "moonbitlang/core/encoding/utf8" as @utf8,
  "moonbitlang/core/json" as @json,
  "moonbitlang/core/strconv" as @strconv,
}

options(
  targets: {
    "sync_native.mbt": [ "native" ],
    "sync_native_wbtest.mbt": [ "native" ],
  },
)